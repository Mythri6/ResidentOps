package com.residentops.model.enums;

// ── Role ──────────────────────────────────────────────────
// GRASP: Information Expert — each role knows what it can do
public enum Role {
    RESIDENT,
    COMMITTEE_ADMIN,
    VENDOR,
    AUDITOR
}
